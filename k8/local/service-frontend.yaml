apiVersion: v1
kind: Service
metadata:
  name: quote-frontend-service
  namespace: quote-k8-java
spec:
  selector:
    app: quote-frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
